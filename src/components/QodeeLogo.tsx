import React from "react";

export const QodeeLogo = ({ className }: { className?: string }) => {
    return (
        <img
            src="/logo.png"
            alt="SkilledCore Logo"
            className={className}
        />
    );
};
